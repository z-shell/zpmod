/* SPDX-License-Identifier: MIT */
/*
 * zmod_imports_placeholder.h — optional stub for out-of-tree builds
 *
 * This header is intentionally left blank. It exists to satisfy the
 * include in `src/module/zpmod.mdh` when building outside the zsh tree
 * and no generated imports header is available. Macros like
 * `mod_import_variable` and `mod_import_function` are defined in
 * `zpmod.mdh` if not provided by the environment.
 */

#pragma once
